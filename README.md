# Kubernetes Lab

This repo is a hands-on Kubernetes SRE lab built around a Redis-backed webhook queue service, used to practice containerization, deployments, health checks, service discovery, observability, scaling, rollbacks, and failure-recovery workflows in a production-like but local learning environment.
